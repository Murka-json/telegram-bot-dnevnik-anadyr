module.exports = {
    bot_token: "token by telegram bot",
    postgresql: {
        database: "name database",
        user: "username by user postgresql",
        password: "password by user postgresql",
        port: 5432,
        host: "host postgresql(domain server or localhost)"
    }
}